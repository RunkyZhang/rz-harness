---
alwaysApply: false
---
# 按钮大小规范

在项目中使用 `el-button` 组件时，请遵循以下大小规范：

## 按钮大小标准

1. **常规按钮**：使用 `size="medium"` 属性
   ```vue
   <el-button type="primary" size="medium">常规按钮</el-button>
   ```

2. **大号按钮**：使用默认大小（不添加size属性）
   ```vue
   <el-button type="primary">大号按钮</el-button>
   ```

3. **小号按钮**：使用 `size="small"` 属性
   ```vue
   <el-button type="primary" size="small">小号按钮</el-button>
   ```

## 示例

项目中添加按钮的示例可参考：[expenseList.vue](mdc:src/views/businessTrip/expenseList.vue)

```vue
<el-button type="primary" size="medium" icon="el-icon-plus" @click="addClick">添加报销单</el-button>
<el-button v-if="canProxy" size="medium" type="warning" icon="el-icon-plus" @click="proxyClick">添加代理报销单</el-button>
```

## 注意事项

- 同一页面中的按钮应保持一致的尺寸，提升用户体验
- 表单内的按钮通常使用小号或常规大小
- 页面主操作按钮可使用大号按钮以突出显示

## 按钮布局规范

### 按钮区域划分

1. **左上区域**：业务功能按钮
   - 如新增、导入、导出等操作按钮
   - 批量操作按钮（如批量删除、批量审核等）

   ```vue
   <div class="left-buttons">
     <el-button type="primary" size="medium" icon="el-icon-plus">新增商品</el-button>
     <el-button size="medium" icon="el-icon-upload2">导入</el-button>
     <el-button size="medium" icon="el-icon-download">导出</el-button>
     <el-dropdown>
       <el-button size="medium">
         批量操作<i class="el-icon-arrow-down el-icon--right"></i>
       </el-button>
       <el-dropdown-menu slot="dropdown">
         <el-dropdown-item>批量删除</el-dropdown-item>
         <el-dropdown-item>批量修改</el-dropdown-item>
       </el-dropdown-menu>
     </el-dropdown>
   </div>
   ```

2. **右上区域**：全局数据相关操作
   - 搜索功能
   - 刷新按钮
   - 设置按钮

   ```vue
   <div class="right-buttons">
     <el-input placeholder="请输入" size="medium" v-model="searchInput" class="search-input">
       <el-button slot="append" size="medium" icon="el-icon-search"></el-button>
     </el-input>
     <el-button icon="el-icon-refresh" size="medium" circle></el-button>
     <el-button icon="el-icon-setting" size="medium" circle></el-button>
   </div>
   ```

### 主按钮使用规范

- **每个按钮区最多使用一个主按钮（type="primary"）**，用于突出页面最重要的操作
- 主按钮通常用于表示新增、保存、确认等正向操作
- 次要操作使用默认样式按钮或其他类型按钮（如 warning、info 等）
- 危险操作建议使用 type="danger" 样式

```vue
<!-- 正确示例 -->
<div class="button-group">
  <el-button type="primary" size="medium">新增商品</el-button>  <!-- 唯一主按钮 -->
  <el-button size="medium">导入</el-button>
  <el-button size="medium">导出</el-button>
</div>

<!-- 错误示例 -->
<div class="button-group">
  <el-button type="primary" size="medium">新增商品</el-button>  <!-- 多个主按钮 -->
  <el-button type="primary" size="medium">导入</el-button>      <!-- 避免这种用法 -->
  <el-button size="medium">导出</el-button>
</div>
```

### 布局示例

完整的按钮区布局参考：

```vue
<div class="operation-bar">
  <div class="left-buttons">
    <el-button type="primary" size="medium" icon="el-icon-plus">新增商品</el-button>
    <el-button size="medium" icon="el-icon-upload2">导入</el-button>
    <el-button size="medium" icon="el-icon-download">导出</el-button>
  </div>
  <div class="right-buttons">
    <el-input placeholder="请输入关键字" size="medium" v-model="searchInput" class="search-input">
      <el-button size="medium" slot="append" icon="el-icon-search"></el-button>
    </el-input>
    <el-button size="medium" icon="el-icon-refresh" circle></el-button>
    <el-button size="medium" icon="el-icon-setting" circle></el-button>
  </div>
</div>
```

配套CSS样式：

```css
.operation-bar {
  display: flex;
  justify-content: space-between;
  margin-bottom: 16px;
}

.left-buttons .el-button {
  margin-right: 8px;
}

.right-buttons {
  display: flex;
  align-items: center;
}

.right-buttons .search-input {
  margin-right: 8px;
  width: 200px;
}
```
