---
description: iOS 颜色规范（ColorRules.h），禁止硬编码颜色
alwaysApply: false
---
# iOS 颜色规范

## 当前颜色定义（ColorRules.h）

基于 `ColorRules.h` 文件中实际定义的颜色标准：

### 文本颜色系列
```objective-c
#define ColorText_0         UIColorHex(@"#0D1632")    // 正文主要文字
#define ColorText_1         UIColorHex(@"#59627D")    // 正文次要文字
#define ColorText_2         UIColorHex(@"#8891AF")    // 注释、辅助信息
#define ColorText_3         UIColorHex(@"#98989A")    // 注释、辅助信息
```

**使用场景：**
- `ColorText_0`：页面标题、重要正文内容、主要信息展示
- `ColorText_1`：次要信息、说明文字、二级标题
- `ColorText_2`：提示信息、辅助说明、时间戳、注释性文字

### 功能颜色系列
```objective-c
#define ColorText_disable   UIColorHex(@"#D1D5E0")    // 禁用状态ICON/文字
#define Color_border        UIColorHex(@"#DFE3F0")    // 边框
#define Color_line          UIColorHex(@"#EAEDF5")    // 分割线、表头颜色
#define Color_bgView        UIColorHex(@"#F5F7FD")    // 页面背景色、禁用背景色、卡片背景
```

**使用场景：**
- `ColorText_disable`：禁用状态的按钮文字、不可点击的图标和文本
- `Color_border`：输入框边框、卡片边框、容器边框
- `Color_line`：列表分割线、区域分隔线、表格表头背景
- `Color_bgView`：页面整体背景、卡片背景、禁用状态背景

### 反馈色系列
```objective-c
#define Color_green         UIColorHex(@"#10B981")    //反馈色-成功
#define Color_yellow        UIColorHex(@"#F59E0B")    //反馈色-警告
#define Color_orange        UIColorHex(@"#D97706")    //
#define Color_red           UIColorHex(@"#EF4444")    //反馈色-错误
#define Color_blue          UIColorHex(@"#416FFC")    //蓝色
#define Color_blue_1        UIColorHex(@"#F0F6FF")    //浅蓝色
```

**使用场景：**
- `Color_green`：成功提示、完成状态、正向操作反馈
- `Color_yellow`：警告信息、需要注意的内容
- `Color_orange`：一般提醒、次要警告
- `Color_red`：错误提示、失败状态、危险操作、删除按钮
- `Color_blue`：链接文字、可点击内容、高亮强调、标签背景
- `Color_blue_1`：浅色背景区域、信息展示卡片背景、弱化的蓝色背景

## 使用规范

### 1. 必须使用标准颜色宏
所有颜色必须使用 `ColorRules.h` 中定义的宏，禁止硬编码颜色值。

❌ **错误示例**
```objective-c
// 硬编码十六进制颜色
label.textColor = UIColorHex(@"#0D1632");
label.textColor = [UIColor colorWithHexString:@"#0D1632"];

// 硬编码 RGB 颜色
label.textColor = [UIColor colorWithRed:0.05 green:0.09 blue:0.20 alpha:1.0];

// 使用系统颜色
label.textColor = [UIColor blackColor];
```

✅ **正确示例**
```objective-c
// 使用标准颜色宏
titleLabel.textColor = ColorText_0;        // 主标题
descLabel.textColor = ColorText_1;         // 说明文字
hintLabel.textColor = ColorText_2;         // 提示信息
disabledLabel.textColor = ColorText_disable; // 禁用文字
linkLabel.textColor = Color_blue;          // 链接文字
errorLabel.textColor = Color_red;          // 错误提示
```

### 2. 文本颜色层级规范
根据信息重要性选择合适的文本颜色层级：

```objective-c
// 层级清晰的信息展示
nameLabel.textColor = ColorText_0;          // 主要：客户名称
phoneLabel.textColor = ColorText_1;         // 次要：联系电话
updateTimeLabel.textColor = ColorText_2;    // 辅助：更新时间
```

### 3. 状态颜色使用规范
正确使用反馈色表达不同的状态和操作结果：

```objective-c
// 成功状态
successLabel.textColor = Color_green;
successIcon.tintColor = Color_green;

// 警告状态
warningLabel.textColor = Color_yellow;
warningBgView.backgroundColor = [Color_yellow colorWithAlphaComponent:0.1];

// 错误状态
errorLabel.textColor = Color_red;
errorBorder.layer.borderColor = Color_red.CGColor;

// 可交互/链接
linkButton.titleColor = Color_blue;
```

### 4. 背景和边框规范
```objective-c
// 页面背景
self.view.backgroundColor = Color_bgView;

// 卡片背景
cardView.backgroundColor = [UIColor whiteColor];  // 卡片使用白色
cardView.layer.borderColor = Color_border.CGColor;
cardView.layer.borderWidth = 1.0;

// 分割线
separatorView.backgroundColor = Color_line;

// 禁用状态
disabledButton.backgroundColor = Color_bgView;
disabledButton.titleLabel.textColor = ColorText_disable;
```

## 检查清单

### 颜色使用检查
- [ ] **[P0]** 是否使用了标准颜色宏定义？
- [ ] **[P0]** 是否避免硬编码颜色值（包括 UIColorHex、RGB、系统颜色）？
- [ ] **[P0]** 文本颜色层级是否正确（主要用 ColorText_0，次要用 ColorText_1，辅助用 ColorText_2）？
- [ ] **[P1]** 状态颜色是否使用正确（成功-绿、警告-黄、错误-红）？
- [ ] **[P1]** 背景色是否符合设计规范（页面背景用 Color_bgView，卡片用白色）？
- [ ] **[P1]** 边框和分割线颜色是否使用标准定义？
- [ ] **[P2]** 禁用状态是否使用了 ColorText_disable？
- [ ] **[P2]** 链接和可交互元素是否使用 Color_blue？

### 常见错误避免
1. ❌ 不要混用老版颜色（TextColor_系列）和新版颜色
2. ❌ 不要自定义相似的颜色值
3. ❌ 不要使用系统颜色（如 blackColor、grayColor）
4. ❌ 不要在代码中直接写 `@"#0D1632"` 等颜色值
5. ❌ 不要为了省事就都用 ColorText_0

## 迁移指南

如果代码中使用了老版颜色，请参考以下映射关系迁移：

| 老版颜色 | 新版颜色 | 说明 |
|---------|---------|------|
| TextColor_0 `#171717` | ColorText_0 `#0D1632` | 主要文字 |
| TextColor_1 `#323234` | ColorText_1 `#59627D` | 次要文字 |
| TextColor_2 `#656567` | ColorText_2 `#8891AF` | 辅助文字 |
| TextColor_bgView `#F0F0F0` | Color_bgView `#F5F7FD` | 背景色 |
| TextColor_line `#F0F0F0` | Color_line `#EAEDF5` | 分割线 |
| TextColor_green `#0DC216` | Color_green `#10B981` | 成功色 |
| TextColor_yellow `#FFB600` | Color_yellow `#F59E0B` | 警告色 |
| TextColor_orange `#FF8B40` | Color_orange `#D97706` | 橙色 |
| TextColor_red `#FA2D19` | Color_red `#EF4444` | 错误色 |
| TextColor_blue `#2F72F5` | Color_blue `#416FFC` | 蓝色/链接 |

**注意：** 新增颜色 `ColorText_disable` 和 `Color_border` 用于禁用状态和边框，老版本中没有对应颜色。
