---
description: 记录项目常用的方法和初始化类
alwaysApply: false
---

# 项目常用方法与初始化类

## 基类与继承

### 页面控制器
- **BaseViewController**：所有业务页面的基类。继承自 `UIViewController`。
  - 默认：`view.backgroundColor = Color_bgView`、导航栏配置、返回按钮、全屏侧滑返回。
  - 子类直接继承 `BaseViewController`，不要继承 `UIViewController`。

### 表格 Cell
- **SFABaseTableCell**：UITableViewCell 基类。在 `initWithStyle:reuseIdentifier:` 中已做：白底、无选中样式、无 accessory，并依次调用 `initSubviews`、`setupSubLayout`。
  - 子类重写 `initSubviews` 添加子视图，重写 `setupSubLayout` 做布局（如 Masonry）。
  - 提供 `indexPath` 属性便于在 Cell 内使用。

### 窗口弹窗
- **SFABaseWindowAlertView**：弹窗整体基类，加在 KeyWindow 上（继承自 UIView）。
  - 展示：`show`、`showInCancelJourneyView:`；关闭：`dissView`；全部关闭：`+ (void)dismissAllAlertView`。
  - 支持 `confirmBlock`、`cancelBlock`、`bgCloseEnable`；可选 `keyboardAdaptiveView` 做键盘适配。
- **SFASpecialAlterView**：继承自 `SFABaseWindowAlertView`，用于**提醒类、消息类**等**屏幕居中**弹窗（含 `backGroundView`、`contentView`、取消/确认按钮等结构）。纯文本提示可用子类如 `SFASpecialAlterTipsView` 等。
---

## 常用方法

### 获取当前控制器
```objective-c
[WWBaseManager getCurrentVC];
```

### Block 中避免循环引用
- 宏定义在 **`FProject/PrefixHeader.pch`**，以该文件为准：
  - `WeakSelf(type)` → `__weak typeof(type) weak##type = type`
  - `StrongSelf(type)` → `__strong typeof(weak##type) strong##type = weak##type`


```objective-c
WeakSelf(self);
[self doSomethingWithCompletion:^{
    StrongSelf(self);
    if (!strongself) { return; }
    // 使用 strongself 访问属性/调方法
}];
```

---

## 判空与打印

### 判空
- **NSDictionary** 判空使用 `MCDictionaryIsEmpty`。
- **NSArray** 判空使用 `MCArrayIsEmpty`。
- **NSString** 判空使用 `MCStringIsEmpty`。

### 字典取字符串（`NSDictionary` / `NSMutableDictionary` Category）
- 需要**统一当字符串用**或后续 `doubleValue` / `integerValue` 时，使用 **`strValueForKey:`**（`NSDictionary+category` / `NSMutableDictionary+category`），**已对 `NSNumber`、`NSNull` 等做兼容**（`NSNumber` 会格式化为字符串），无需再手写 `isKindOfClass:[NSNumber class]` 分支。
- 取不到或空时返回 `@""`，配合 **`MCStringIsEmpty`** 判断即可。

### 打印
- 默认使用 **DEBUGLog**；仅在确有需要时手动使用 `NSLog`。

---

- 不输出无关解释，除非明确告知需要输出；有多种方案的情况，只输入最优方案；只返回必要代码。

---
