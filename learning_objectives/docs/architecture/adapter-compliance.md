# Adapter Compliance Matrix

> 用途：记录当前 harness 对外部工具 / agent harness 的真实支持状态。状态表是事实约束，不是营销清单；未列验证命令的能力不得宣称可用。

| Adapter | State | Supported assets | Unsupported or different surfaces | Install or onramp | Verification command | Risk notes | Owner | Last verified | Source docs |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Codex | Adapter-backed | `AGENTS.md`, `.codex/hooks.json`, `hooks/codex-*.sh`, `scripts/harness-sensor-runner.sh`, `config/agent-registry.yml`, generated Codex agent TOML, optional ECC sidecar wrapper | ECC / Claude-style hook parity is not assumed; generated agents still consume harness gates and project hooks still require Codex trust | Open repo in Codex, review project hooks; generate agents with `scripts/codex-agent-generator.sh --dry-run`; optional ECC uses `artifacts/vendor/ECC` or `SFA_ECC_HOME` | `scripts/team-rollout-preflight.sh`; `scripts/agent-registry-gate.sh`; `scripts/codex-agent-generator-test.sh`; `scripts/ecc/ecc-sidecar.sh status` | ECC is optional; generated Codex agents must not overwrite project `AGENTS.md`, `.codex/hooks.json`, or global `~/.codex` without second confirmation | Orchestrator | 2026-07-03 | `docs/onboarding.md`, `docs/architecture/ecc-integration.md`, `docs/architecture/agent-registry.md` |
| Cursor | Adapter-backed | `.cursor/hooks.json`, `.cursor/rules/*.mdc`, shared rules, sensor runner | Cursor hook event names differ from Codex; Trusted workspace is required | Open trusted workspace and confirm hooks are loaded | `scripts/team-rollout-preflight.sh` | Cursor rules are adapters only; source of truth remains `AGENTS.md`, `docs/`, `rules/`, `scripts/` | Orchestrator | 2026-06-15 | `docs/onboarding.md` |
| OpenCode | Adapter-backed | `opencode.json`, `.opencode/agents/*`, `config/agent-registry.yml`, generated local config | Versioned config does not include `external_directory`; local config is ignored; OpenCode agent files are adapter implementations, not source of truth | `source config/repos.local.sh`; `scripts/generate-opencode-local-config.sh`; `OPENCODE_CONFIG=config/opencode.local.json opencode .` | `scripts/team-rollout-preflight.sh`; `scripts/agent-registry-gate.sh` | Generated local config may contain absolute paths and must not be committed; agent definitions must stay registered in `config/agent-registry.yml` | Orchestrator | 2026-07-03 | `docs/onboarding.md`, `docs/decision-log/2026-05-19-opencode-adapter.md`, `docs/architecture/agent-registry.md` |
| CodeGraph | Reference-only | MCP symbol search / node / trace after preflight | Missing symbols or no output are not no-impact proof | `scripts/codegraph-preflight.sh --sync <repo>` | `scripts/codegraph-evidence-gate.sh <evidence-file>` | Always keep `rg` / direct source fallback evidence | Orchestrator | 2026-06-15 | `docs/onboarding.md`, `templates/codegraph-evidence.md` |
| GitNexus | Reference-only | Impact and diff risk sensors | Unavailable sensor downgrades to local evidence; it does not block ordinary narrow changes by itself | `scripts/gitnexus-impact.sh`; `scripts/gitnexus-detect-changes.sh` | `scripts/gitnexus-detect-changes.sh --help` | HIGH / CRITICAL findings require explicit review before merge | Orchestrator | 2026-06-15 | `docs/README.md`, `templates/pre-pr-review.md` |
| Browser / Playwright | Instruction-backed | PC smoke lane, local proxy proof, screenshots / traces | Browser proof does not replace API, DB, or release checks | `lanes/pc-e2e-smoke.md` | `scripts/local-routing-gate.sh <routing.yml>` | Do not record plaintext credentials, tokens, or cookies | Orchestrator | 2026-06-15 | `lanes/pc-e2e-smoke.md`, `templates/pc-e2e-smoke-report.md` |
| Lark CLI | Reference-only | Feishu docs, sheets, wiki, whiteboard, release coordination | Real sends / edits require task-specific confirmation and readback | Follow the relevant `lark-*` skill and record evidence | Section-scope readback command for the edited document | Never persist secrets; DB / release decisions still use harness gates | Orchestrator | 2026-06-15 | `AGENTS.md`, `docs/onboarding.md` |
| ECC sidecar | Instruction-backed | Optional `consult`, `harness-audit`, `observability-ready`, `install-plan --dry-run`, `codex-sync-dry-run` via `scripts/ecc/ecc-sidecar.sh` | Not a required dependency; no apply install, no global sync, no auto-fix through wrapper | Clone ECC to `artifacts/vendor/ECC` or set `SFA_ECC_HOME` | `scripts/ecc/ecc-sidecar-test.sh`; `scripts/ecc/ecc-sidecar.sh status` | Missing ECC must not break normal harness gates | Orchestrator | 2026-06-15 | `docs/architecture/ecc-integration.md` |
